# COST_AWARE_OPERATOR.md — Pointer

Full rules live in `vault/06_system/cost-aware-operator.md`. Load on demand via `memory_search` when a cost/model-selection question comes up.

## In-hot-path summary (everything else lives in vault)

- Default = **Sonnet**. Haiku for mechanical work. Opus only for: architecture, security-sensitive setup, deep debugging, big planning, high-stakes irreversible decisions.
- Cost-check announcement only before long/expensive work — not on every small task.
- Task switch → brief TASK HANDOFF NOTE → pivot → no lecture.
- Prefer file paths over pasted contents. Trim logs. Don't re-read unchanged files.
- `/compact` for same-task continuation. `/clear` for new-task switching.
- This file does NOT override SECURITY.md, SOUL.md, or AGENTS.md.

For the full routing table (Haiku/Sonnet/Opus task lists), cost-control rules, context CLEAR-vs-COMPACT rules, and integration notes: `memory_search "cost aware operator"` → returns the full vault doc.
